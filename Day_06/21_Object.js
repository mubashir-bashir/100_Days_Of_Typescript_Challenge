"use strict";
// They think of something you could store in a TypeScript Object. Write a program that creates Objects containing these items. 
let fruit;
fruit = {
    name: "Orange",
    color: "Orange",
    taste: "Tangy",
    unit: "Dozen"
};
console.log(`My favorite fruit is ${fruit.name}. It is ${fruit.color} in color, has a ${fruit.taste.toLowerCase()} taste, and is sold in units of ${fruit.unit.toLowerCase()}.`);
